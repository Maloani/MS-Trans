<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_rdc">

    <!-- Logo de l'application -->
    <!-- Logo animé (rotation) -->
    <ImageView
        android:id="@+id/logoSplash"
        android:layout_width="140dp"
        android:layout_height="140dp"
        android:src="@drawable/logo"
        android:layout_centerInParent="true" />

    <!-- Slogan de l'application -->
    <TextView
        android:id="@+id/slogan"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Votre transport en toute confiance en RDC."
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_below="@id/logoSplash"
        android:layout_marginTop="20dp"
        android:padding="10dp"
        android:shadowColor="#80000000"
        android:shadowDx="1"
        android:shadowDy="1"
        android:shadowRadius="2"/>



    <!-- Bouton Commander un moyen de transport -->
    <Button
        android:id="@+id/orderTransportButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🚖 Commander un moyen de transport"
        android:layout_centerHorizontal="true"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="30dp"
        android:padding="15dp"
        android:backgroundTint="#00796B"
        android:textColor="#FFFFFF"/>



</RelativeLayout>
